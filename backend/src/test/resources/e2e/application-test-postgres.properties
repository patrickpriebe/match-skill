# Template properties for running e2e tests against isolated PostgreSQL and Redis
# Populated dynamically by LivePostgresRedisServerRunner and PostgresRedisLiveIntegrationTest
# Default isolated local ports:
# test.db.host=localhost
# test.db.port=5432
# test.db.name=matchskill
# test.db.user=matchskill
# test.db.pass=matchskill
# test.redis.host=localhost
# test.redis.port=6379
# server.port=8089
